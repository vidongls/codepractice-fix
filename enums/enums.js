const userEnum = ["USER", "ADMIN", "SUPER_ADMIN"];

const levelEnum = ["EASY", "MEDIUM", "HARD", "EXPERT"];

const challengeStatusEnum = ["NEW", "PROCESSING", "SUCCESS"];

module.exports = {
	userEnum,
	levelEnum,
	challengeStatusEnum,
};
